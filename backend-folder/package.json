{
  "name": "backend-folder",
  "version": "1.0.0",
  "description": "Puppeteer + stealth scraper, run on a schedule via GitHub Actions (.github/workflows/daily-scrape.yml) as the primary scraper, with the Vercel cron (front-end/api/scrape.js) as a secondary heartbeat/backup. Populates Supabase with menu data + weekly hours from new.dineoncampus.com.",
  "main": "run-scrape.js",
  "scripts": {
    "scrape": "node run-scrape.js",
    "scrape:force": "node run-scrape.js --force"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.3.1",
    "puppeteer": "^24.30.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
